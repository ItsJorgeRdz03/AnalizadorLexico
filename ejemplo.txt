var s = 1213;
const s1 = "hola";
let s2 = s1;

myfuncion(1, 2);

objeto.atributo;

objeto.myfuncion(asa);

function calc() {
   var x = 1;

   if (a == ture) {
     var valor = "saludos";
   }
}

(e) => {
   myfuncion();
}

if (b == ture) {
  var valor = "saludos";
} else {
  llamar(v1);
}

for (let id = 1; id < 5; id = id + 1) {
   myobjeto.setAtr(i);
}